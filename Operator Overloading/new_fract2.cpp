#include<iostream>
using namespace std;
class Fraction{
    private:
        long num,den;
        long gcd(long,long);
    public:
        inline Fraction(int = 0,int = 1);
        inline Fraction()
}